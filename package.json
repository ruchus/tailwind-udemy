{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tailwindcss-cli build -i ./src/styles.css -o ./css/styles.css"
  },
  "devDependencies": {
    "tailwindcss": "^3.1.6"
  }
}
